node_modules/
*.log

# The push credential lives in .git/egt-push-token, which git never tracks.
# Anything that looks like a token in the working tree is a mistake:
*token*.txt
*.pat
.env
RELEASE_NOTES.md
release-notes.md

# Where a Cowork session drops plans and notes on this repo. Not part of the plugin,
# and release.mjs refuses to run with anything untracked lying around.
Claude outputs/
